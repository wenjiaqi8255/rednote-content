# Rednote Post - UI 设计文档

**版本**: 1.0
**最后更新**: 2026-03-18
**设计师**: Wen Jiaqi
**项目**: 小红书卡片生成器

---

## 📋 目录

1. [项目概述](#项目概述)
2. [设计原则](#设计原则)
3. [页面层级](#页面层级)
4. [组件设计](#组件设计)
5. [用户流程](#用户流程)
6. [交互设计](#交互设计)
7. [响应式设计](#响应式设计)
8. [设计系统](#设计系统)

---

## 项目概述

### 核心功能
Rednote Post 是一个基于 Web 的小红书风格卡片生成器，支持 Markdown 和 LaTeX 数学公式渲染。用户可以：
- 创建和管理多个卡片会话
- 使用 Markdown 编写内容
- 选择 8 种不同的主题风格
- 实时预览卡片效果
- 导出高质量 PNG 图片

### 技术栈
- **框架**: Next.js 16.1.6 + React 19
- **样式**: Tailwind CSS 3.4.17
- **状态管理**: React Context API
- **数据持久化**: Local Storage
- **渲染引擎**: markdown-it + KaTeX

---

## 设计原则

### 1. 极简主义 (Minimalism)
- **减少视觉噪音**: 使用大量留白，避免过度装饰
- **清晰的信息层级**: 通过字体大小、颜色深浅建立层级
- **Notion 风格**: 借鉴 Notion 的简洁线条和卡片设计

### 2. 功能优先 (Function over Form)
- **快速创作**: 最小化从创建到导出的步骤
- **即时反馈**: 实时预览，无需等待
- **无干扰**: 专注于内容创作，界面元素不抢夺注意力

### 3. 渐进式展示 (Progressive Disclosure)
- **基础功能始终可见**: 表单和预览始终在主界面
- **高级功能按需出现**: 设置、导出等功能在需要时才展示
- **移动优先**: 在小屏幕上通过菜单折叠次要内容

### 4. 可访问性 (Accessibility)
- **足够的点击区域**: 所有按钮至少 44x44px
- **清晰的焦点状态**: 使用灰色边框和阴影指示焦点
- **颜色对比度**: 遵循 WCAG AA 标准，确保文字可读性

---

## 页面层级

### 整体布局

```
┌─────────────────────────────────────────────────────┐
│ Mobile Header (仅移动端)                            │
├───────────────────┬─────────────────────────────────┤
│                   │                                 │
│   Sidebar         │      Main Content               │
│   (SessionList)   │      (SessionDetail)             │
│                   │                                 │
│   - 固定宽度        │   - 自适应宽度                   │
│   - 320px (桌面)    │   - flex-1                      │
│   - 全屏(移动)      │                                 │
│                   │                                 │
└───────────────────┴─────────────────────────────────┘
```

### 层级结构

#### Level 1: 应用容器 (App Container)
- **文件**: `app/page.tsx`
- **组件**: `HomeContent`
- **背景**: `bg-gray-50` (浅灰色背景)
- **布局**: Flexbox 水平布局

#### Level 2: 侧边栏 (Sidebar)
- **文件**: `components/SessionList.tsx`
- **宽度**: 320px (桌面端)
- **背景**: `bg-white`
- **边框**: 右侧 `border-gray-200`
- **移动端**: 固定定位，可折叠

#### Level 3: 主内容区 (Main Content)
- **文件**: `components/SessionDetail.tsx`
- **宽度**: `flex-1` (自适应)
- **背景**: `bg-white` (白色卡片)
- **阴影**: `shadow-sm` (轻微阴影)

---

## 组件设计

### 1. MobileHeader 组件

**文件**: `components/MobileHeader.tsx`

#### 设计规格
- **高度**: 56px (固定)
- **背景**: `bg-white`
- **边框**: 底部 `border-gray-200`
- **定位**: `sticky top-0`
- **Z-Index**: 50

#### 元素构成
```
┌────────────────────────────────────┐
│ Rednote Card Generator    [≡]     │
│                                    │
└────────────────────────────────────┘
```

- **标题区**:
  - 字体大小: `text-lg` (18px)
  - 字重: `font-semibold`
  - 颜色: `text-gray-900`
  - 截断: `truncate` (超出显示省略号)

- **菜单按钮**:
  - 尺寸: 48x48px (点击区域)
  - 图标尺寸: 24x24px
  - 颜色: `text-gray-600`
  - Hover: `text-gray-900` + `bg-gray-100`
  - 圆角: `rounded-lg`
  - 图标切换:
    - 打开: 汉堡菜单图标 (三条横线)
    - 关闭: X 图标

#### 状态变化
- **默认**: 显示汉堡菜单图标
- **菜单打开**:
  - 图标切换为 X
  - ARIA 属性: `aria-expanded="true"`
  - ARIA 标签: "关闭菜单"

---

### 2. SessionList 组件

**文件**: `components/SessionList.tsx`

#### 设计规格
- **布局**: 垂直 Flexbox (`flex flex-col`)
- **高度**: `h-full` (填满容器)
- **背景**: `bg-white`
- **内边距**: `px-6 py-4`

#### 结构分解

##### 2.1 头部区域 (Header)
```
┌────────────────────────────────────┐
│ 我的作品                             │
│ 3 个卡片                            │
│ ┌──────────────────────────────────┐│
│ │      [+ 创建新卡片]              ││
│ └──────────────────────────────────┘│
└────────────────────────────────────┘
```

- **标题**:
  - 字体大小: `text-2xl` (24px)
  - 字重: `font-semibold`
  - 颜色: `text-gray-900`
  - 底部间距: `mb-1`

- **副标题**:
  - 字体大小: `text-sm` (14px)
  - 颜色: `text-gray-500`
  - 底部间距: `mb-5`

- **创建按钮**:
  - 样式: `btn btn-primary`
  - 宽度: `w-full` (100%)
  - 最小高度: 48px
  - 阴影: `shadow-sm`
  - Hover: `shadow-md`
  - 图标: + 号 (20x20px)
  - 文字: "创建新卡片"

##### 2.2 列表区域 (List)
**空状态**:
```
┌────────────────────────────────────┐
│                                    │
│             ⋄                      │
│         开始创作                     │
│    点击上方按钮创建你的第一张卡片      │
│                                    │
└────────────────────────────────────┘
```

- **图标**:
  - 内容: "⋄" (菱形符号)
  - 大小: `text-6xl` (60px)
  - 透明度: `opacity-30`

- **标题**:
  - 字体大小: `text-xl` (20px)
  - 字重: `font-semibold`
  - 颜色: `text-gray-900`
  - 底部间距: `mb-2`

- **描述**:
  - 字体大小: `text-sm` (14px)
  - 颜色: `text-gray-500`

**有内容状态**:
```
┌────────────────────────────────────┐
│ 我的效率工具集锦          [×]       │
│ [default] 2小时前      [已生成]     │
│ ──────────────────────────────────│
│ 学习笔记               [×]       │
│ [neo-brutalism] 刚刚                 │
└────────────────────────────────────┘
```

##### 2.3 单个卡片项 (Card Item)
**容器**:
- 内边距: `p-4`
- 背景:
  - 默认: `bg-white`
  - 当前选中: `bg-gray-50`
  - Hover: `hover:bg-gray-50`
- 圆角: 无 (Notion 风格)
- 动画: `animate-fade-in-up` (渐入上升)
- 动画延迟: `index * 50ms` (交错动画)

**标题行**:
```
┌────────────────────────────────────┐
│ 我的效率工具集锦              [×]  │
└────────────────────────────────────┘
```

- **标题**:
  - 字重: `font-semibold`
  - 颜色: `text-gray-900`
  - 右边距: `pr-2` (为删除按钮留空间)
  - 宽度: `flex-1` (占据剩余空间)

- **删除按钮**:
  - 点击区域: 44x44px
  - 背景: 无
  - Hover: `bg-gray-200`
  - 圆角: `rounded-md`
  - 图标: × 号 (16x16px)
  - 颜色: `text-gray-500`
  - 确认对话框: "确定要删除这个会话吗？此操作无法撤销。"

**元信息行**:
```
┌────────────────────────────────────┐
│ [default] 2小时前                   │
└────────────────────────────────────┘
```

- **主题标签**:
  - 内边距: `px-2 py-1`
  - 圆角: `rounded-md`
  - 字重: `font-medium`
  - 字母间距: `uppercase tracking-wide`
  - 字体大小: `text-xs` (12px)
  - 颜色:
    - 当前选中: `bg-gray-200` + `text-gray-700`
    - 未选中: `bg-gray-100` + `text-gray-600`
  - 右边距: `gap-2` (与时间戳的间距)

- **时间戳**:
  - 字体大小: `text-xs` (12px)
  - 颜色: `text-gray-400`
  - 格式化规则:
    - < 1分钟: "刚刚"
    - < 60分钟: "X分钟前"
    - < 24小时: "X小时前"
    - < 7天: "X天前"
    - ≥ 7天: "2024/03/18" (中文格式)

**图片状态指示器**:
```
┌────────────────────────────────────┐
│ 🖷 已生成                           │
└────────────────────────────────────┘
```

- **图标**: 图片图标 (14x14px)
- **间距**: `gap-1.5`
- **字体大小**: `text-xs` (12px)
- **颜色**: `text-gray-500`
- **字重**: `font-medium`
- **顶部间距**: `mt-2`

**分隔线**:
- 高度: 1px
- 颜色: `border-gray-100` (极淡的灰色)
- 位置: 卡片之间 (最后一个卡片除外)

---

### 3. SessionDetail 组件

**文件**: `components/SessionDetail.tsx`

#### 设计规格
- **布局**: 垂直 Flexbox (`flex flex-col`)
- **高度**: `h-full`
- **背景**: `bg-white`
- **圆角**: `rounded-lg`
- **阴影**: `shadow-sm`
- **溢出**: `overflow-hidden` (圆角裁剪)

#### 结构分解

##### 3.1 空状态 (Empty State)
```
┌────────────────────────────────────┐
│                                    │
│             ⋄                      │
│      请选择或创建一个会话             │
│   从左侧列表选择现有会话，或创建新会话开始│
│                                    │
└────────────────────────────────────┘
```

- **容器**:
  - 布局: 居中对齐 `flex items-center justify-center`
  - 边框: `border border-gray-200`
  - 最小高度: `h-full`

- **图标**: 与 SessionList 相同

##### 3.2 有内容状态
```
┌────────────────────────────────────┐
│ 我的效率工具集锦                      │
│ 最后更新: 2024/03/18 14:30:00       │
├────────────────────────────────────┤
│ 编辑内容                            │
│ [表单区域]                           │
│                                    │
│ 卡片预览                            │
│ [预览区域]                           │
│                                    │
│ 已保存的图片                         │
│ [图片区域]                           │
└────────────────────────────────────┘
```

**头部区域**:
- **背景**: `bg-gray-50` (浅灰色)
- **内边距**: `p-4`
- **边框**: 底部 `border-gray-200`
- **标题**:
  - 字体大小: `text-lg` (18px)
  - 字重: `font-semibold`
  - 颜色: `text-gray-900`
  - 底部间距: 无
- **副标题**:
  - 字体大小: `text-sm` (14px)
  - 颜色: `text-gray-500`

**内容区域**:
- **布局**: 垂直 Flexbox
- **内边距**: `p-6`
- **间距**: `space-y-6` (每个区块之间 24px)
- **滚动**: `overflow-y-auto`

**区块标题**:
- **字体大小**: `text-md` (16px)
- **字重**: `font-semibold`
- **颜色**: `text-gray-800`
- **底部间距**: `mb-3`

---

### 4. Form 组件

**文件**: `components/Form.tsx`

#### 设计规格
- **间距**: `space-y-6` (表单字段之间 24px)
- **动画**: 无 (直接响应)

#### 表单字段设计

##### 4.1 标题输入 (可选)
```
┌────────────────────────────────────┐
│ 标题（可选）                         │
│ ┌──────────────────────────────────┐│
│ │ 例如：效率工具集锦                ││
│ └──────────────────────────────────┘│
└────────────────────────────────────┘
```

- **标签**:
  - 字体大小: `text-sm` (14px)
  - 字重: `font-medium`
  - 颜色: `text-gray-700`
  - 底部间距: `mb-2`

- **输入框**:
  - 宽度: `w-full`
  - 内边距: `px-4 py-3` (水平 16px, 垂直 12px)
  - 边框: `border-gray-300`
  - 圆角: `rounded-md` (6px)
  - 字体大小: `text-base` (16px)
  - 焦点状态:
    - 边框: `focus:ring-gray-400`
    - 描边: `focus:border-gray-400`
    - 轮廓: `focus:ring-1`
  - 占位符: "例如：效率工具集锦"

##### 4.2 Markdown 输入 (必填)
```
┌────────────────────────────────────┐
│ Markdown 内容 *                    │
│ ┌──────────────────────────────────┐│
│ │ # 标题                           ││
│ │                                  ││
│ │ 这是正文内容...                   ││
│ │                                  ││
│ └──────────────────────────────────┘│
│ 💡 推荐主题：neo-brutalism           │
└────────────────────────────────────┘
```

- **文本域**:
  - 行数: 12 行
  - 字体: `font-mono` (等宽字体)
  - 字体大小: 16px (防止 iOS 自动缩放)
  - 其他与标题输入框相同

- **占位符**:
  ```
  # 标题

  这是正文内容...

  ---

  这是第二张卡片
  ```

- **推荐提示** (条件渲染):
  - 触发条件: 内容长度 > 10 字符且推荐主题非 default
  - 间距: `mt-2`
  - 字体大小: `text-sm` (14px)
  - 颜色: `text-gray-600`
  - 图标: 💡
  - 内容: "推荐主题：[主题名]"

##### 4.3 主题选择
```
┌────────────────────────────────────┐
│ 主题风格                            │
│ ┌──────────────────────────────────┐│
│ │ 默认                          ▼│
│ └──────────────────────────────────┘│
└────────────────────────────────────┘
```

**选项列表**:
1. `default` - 默认
2. `playful-geometric` - 趣味几何
3. `neo-brutalism` - 新野兽派
4. `botanical` - 植物
5. `professional` - 商务
6. `retro` - 复古
7. `terminal` - 终端
8. `sketch` - 手绘

##### 4.4 分页模式
```
┌────────────────────────────────────┐
│ 分页模式                            │
│ ┌──────────────────────────────────┐│
│ │ 单卡片模式（完整内容）          ▼│
│ └──────────────────────────────────┘│
└────────────────────────────────────┘
```

**选项列表**:
1. `separator` - 单卡片模式（完整内容）
2. `auto-split` - 多卡片模式（自动切分）

##### 4.5 提交按钮
```
┌────────────────────────────────────┐
│      生成小红书卡片                   │
└────────────────────────────────────┘
```

- **宽度**: `w-full`
- **背景**: `bg-gray-900` (深灰色)
- **文字颜色**: `text-white`
- **字重**: `font-semibold`
- **内边距**: `py-4 px-6`
- **字体大小**: `text-lg` (18px)
- **圆角**: `rounded-md`
- **Hover**: `bg-gray-800`
- **焦点**:
  - 轮廓: `focus:ring-2 focus:ring-gray-400`
  - 偏移: `focus:ring-offset-2`
- **禁用状态**:
  - 背景: `disabled:bg-gray-300`
  - 光标: `disabled:cursor-not-allowed`
  - 触发条件: `isLoading || !markdown.trim()`
- **文字**:
  - 加载中: "生成中..."
  - 正常: "生成小红书卡片"

---

### 5. CardPreview 组件

**文件**: `components/CardPreview.tsx`

#### 设计规格
- **功能**: 实时预览 + 图片捕获
- **布局**: 垂直
- **间距**: 预览和按钮之间 `space-y-4`

#### 组件结构
```
┌────────────────────────────────────┐
│ [卡片预览区域]                       │
│                                    │
│ ┌──────────────────────────────────┐│
│ │   # 标题                          ││
│ │   这是正文内容...                 ││
│ │                                  ││
│ └──────────────────────────────────┘│
│                                    │
│ [📷 捕获图片]  [⬇️ 下载图片]         │
└────────────────────────────────────┘
```

##### 5.1 预览容器
- **背景**: `bg-white`
- **边框**: `border-gray-200`
- **圆角**: `rounded-lg`
- **阴影**: `shadow-sm`
- **内边距**: `p-8`
- **最大宽度**: `max-w-md`
- **居中**: `mx-auto`

##### 5.2 按钮组
**布局**: 水平排列，间距 `gap-3`

**捕获按钮**:
- **背景**: `bg-gray-900`
- **文字**: `text-white`
- **图标**: 📷 相机图标
- **文字**: "捕获图片"
- **Hover**: `bg-gray-800`
- **禁用**: 捕获中时禁用
- **加载状态**: 显示 "捕获中..."

**下载按钮**:
- **背景**: `bg-white`
- **边框**: `border-gray-300`
- **文字**: `text-gray-700`
- **图标**: ⬇️ 下载图标
- **文字**: "下载图片"
- **Hover**: `bg-gray-50`
- **禁用**: 无图片时禁用

##### 5.3 已保存图片显示
```
┌────────────────────────────────────┐
│ 已保存的图片                         │
│ ┌──────────────────────────────────┐│
│ │                                  ││
│ │       [保存的图片]                ││
│ │                                  ││
│ └──────────────────────────────────┘│
└────────────────────────────────────┘
```

- **容器背景**: `bg-gray-50`
- **容器边框**: `border-gray-200`
- **容器圆角**: `rounded-lg`
- **内边距**: `p-4`
- **图片**: `w-full h-auto`
- **图片圆角**: `rounded-lg`
- **图片阴影**: `shadow-sm`
- **图片边框**: `border-gray-200`

---

## 用户流程

### 1. 新用户流程 (First-Time User)

```
开始
  │
  ├─> 查看空状态 (SessionList)
  │   - 显示欢迎信息
  │   - 引导创建第一张卡片
  │
  ├─> 点击 "创建新卡片"
  │   - 自动创建新会话
  │   - 移动端: 自动关闭菜单
  │
  ├─> 输入内容 (Form)
  │   - 输入标题 (可选)
  │   - 输入 Markdown 内容
  │   - 选择主题 (可选，自动推荐)
  │   - 选择分页模式
  │
  ├─> 实时预览 (CardPreview)
  │   - 自动渲染卡片
  │   - 显示推荐主题提示
  │
  ├─> 点击 "生成小红书卡片"
  │   - 生成卡片 HTML
  │   - 保存到 Local Storage
  │   - 显示捕获按钮
  │
  ├─> 捕获图片
  │   - 使用 html2canvas 截图
  │   - 保存到 Local Storage
  │   - 显示下载按钮
  │
  ├─> 下载图片
  │   - 浏览器下载 PNG
  │   - 文件名格式: `rednote-card-[timestamp].png`
  │
  └─> 返回列表
      - 保存的图片显示在详情页
      - 列表中显示 "已生成" 标记
```

### 2. 编辑现有会话流程

```
选择会话
  │
  ├─> 侧边栏点击卡片
  │   - 高亮当前卡片
  │   - 加载会话内容到表单
  │   - 加载保存的图片
  │
  ├─> 修改内容
  │   - 表单回显已有数据
  │   - 实时更新预览
  │
  ├─> 保存更改
  │   - 自动保存到 Local Storage
  │   - 更新时间戳
  │   - 重新生成预览
  │
  └─> 捕获新图片
      - 覆盖之前的图片
      - 更新列表状态
```

### 3. 删除会话流程

```
点击删除按钮 (×)
  │
  ├─> 显示确认对话框
  │   - "确定要删除这个会话吗？"
  │   - "此操作无法撤销。"
  │
  ├─> 确认删除
  │   - 从 Local Storage 删除
  │   - 如果是当前会话，清空详情页
  │   - 列表动画移除卡片
  │
  └─> 取消删除
      - 保持不变
```

### 4. 移动端菜单流程

```
打开菜单
  │
  ├─> 点击汉堡菜单图标
  │   - 侧边栏从左侧滑入
  │   - 显示背景遮罩
  │   - 图标切换为 X
  │
  ├─> 创建新卡片
  │   - 自动关闭菜单
  │   - 移动到详情页
  │
  ├─> 选择会话
  │   - 自动关闭菜单
  │   - 移动到详情页
  │
  └─> 点击背景遮罩 / X 图标
      - 菜单滑出
      - 移除背景遮罩
      - 图标切换为汉堡菜单
```

---

## 交互设计

### 1. 微交互 (Micro-interactions)

#### 1.1 按钮悬停 (Hover)
**主要按钮**:
- 阴影增强: `shadow-sm` → `shadow-md`
- 背景加深: `bg-gray-900` → `bg-gray-800`
- 过渡: `transition-colors` (200ms)

**次要按钮**:
- 背景变化: `bg-white` → `bg-gray-50`
- 边框保持: `border-gray-300`

#### 1.2 卡片悬停
- 背景变化: `bg-white` → `hover:bg-gray-50`
- 过渡: `transition-all` (300ms)
- 删除按钮显现: `hover:bg-gray-200`

#### 1.3 焦点状态 (Focus)
所有交互元素都有清晰的焦点状态:
- 输入框: `focus:ring-1 focus:ring-gray-400`
- 按钮: `focus:ring-2 focus:ring-gray-400 focus:ring-offset-2`
- 下拉菜单: `focus:ring-1 focus:ring-gray-400`

### 2. 动画设计

#### 2.1 列表进入动画
```css
@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in-up {
  animation: fade-in-up 0.3s ease-out;
}
```

**交错延迟**: 每个列表项延迟 `index * 50ms`

#### 2.2 移动端菜单滑入
```css
.transform {
  transition: transform 300ms ease-in-out;
}

.translate-x-full {
  transform: translateX(-100%);
}

.translate-x-0 {
  transform: translateX(0);
}
```

**时序**:
1. 点击菜单按钮
2. 背景遮罩淡入 (150ms)
3. 侧边栏滑入 (300ms)

### 3. 加载状态

#### 3.1 按钮加载状态
- **文字变化**: "生成小红书卡片" → "生成中..."
- **禁用**: `disabled:bg-gray-300`
- **光标**: `disabled:cursor-not-allowed`
- **无加载动画**: 保持简洁

#### 3.2 图片捕获状态
- **按钮禁用**: 防止重复点击
- **按钮文字**: "捕获图片" → "捕获中..."
- **无进度条**: 快速操作 (<2s)，无需进度指示

### 4. 错误处理

#### 4.1 表单验证
- **空内容**: 提交按钮禁用 `disabled={!markdown.trim()}`
- **无错误提示**: 通过按钮禁用状态引导用户

#### 4.2 删除确认
- **对话框**: `window.confirm()` 原生对话框
- **消息**: "确定要删除这个会话吗？此操作无法撤销。"
- **取消**: 保持不变
- **确认**: 执行删除

#### 4.3 Local Storage 失败
- **控制台日志**: 错误信息输出到控制台
- **用户界面**: 无提示（降级体验）
- **降级策略**: 会话数据仅在当前会话有效

---

## 响应式设计

### 1. 断点设计

#### 1.1 桌面端 (≥768px)
```
┌─────────────────────────────────────────────┐
│ Body Background (bg-gray-50)               │
│ ┌──────────┬──────────────────────────┐    │
│ │Sidebar   │ Main Content             │    │
│ │(320px)   │ (flex-1)                 │    │
│ │          │                          │    │
│ │          │                          │    │
│ └──────────┴──────────────────────────┘    │
└─────────────────────────────────────────────┘
```

- **侧边栏**:
  - 定位: `md:relative`
  - 显示: `md:translate-x-0`
  - 宽度: 固定 320px
  - 顶部内边距: `md:pt-0`

- **主内容**:
  - 宽度: `flex-1`
  - 无最大宽度限制

- **Mobile Header**:
  - 显示: `md:hidden`

#### 1.2 移动端 (<768px)
```
┌─────────────────────────────────────┐
│ Mobile Header (56px)                │
├─────────────────────────────────────┤
│ Body Background (bg-gray-50)        │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ Sidebar (全屏, 可折叠)          │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ Main Content                    │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

- **侧边栏**:
  - 定位: `fixed inset-y-0 left-0 z-40`
  - 宽度: `w-80` (320px)
  - 变换:
    - 默认: `-translate-x-full` (隐藏)
    - 打开: `translate-x-0` (显示)
  - 顶部内边距: `pt-16` (为 Header 留空间)
  - 过渡: `transition-transform duration-300`

- **背景遮罩**:
  - 显示条件: `isMobileMenuOpen && md:hidden`
  - 样式: `fixed inset-0 bg-black bg-opacity-50 z-30`
  - 点击: 关闭菜单

- **Mobile Header**:
  - 显示: `md:hidden`
  - 定位: `sticky top-0 z-50`

### 2. 触摸优化

#### 2.1 最小点击区域
所有可点击元素至少 44x44px:
- 按钮: `minWidth: '44px', minHeight: '44px'`
- 删除按钮: `style={{ minWidth: '44px', minHeight: '44px' }}`
- 菜单按钮: `style={{ minWidth: '44px', minHeight: '44px' }}`

#### 2.2 触摸反馈
- **Hover**: 在触摸设备上变为 active 状态
- **背景**: `hover:bg-gray-100` 在触摸时立即显示
- **无延迟**: 使用 `touch-action: 'manipulation'` 防止双击缩放

#### 2.3 滚动优化
- **弹性滚动**: iOS Safari 的 `-webkit-overflow-scrolling: touch`
- **滚动链**: 使用 `overflow-y-auto` 而非 `overflow: scroll`
- **滚动条**: 自定义滚动条样式（Tailwind 默认）

---

## 设计系统

### 1. 颜色系统 (Color System)

#### 1.1 主色调 (Primary Colors)
```css
/* 灰色系统 - 用于主要操作和强调 */
--gray-50:  #f9fafb  /* 背景浅色 */
--gray-100: #f3f4f6  /* 背景灰色 */
--gray-200: #e5e7eb  /* 边框、分隔线 */
--gray-300: #d1d5db  /* 输入框边框 */
--gray-400: #9ca3af  /* 焦点环 */
--gray-500: #6b7280  /* 次要文字 */
--gray-600: #4b5563  /* 常规文字 */
--gray-700: #374151  /* 深色文字 */
--gray-800: #1f2937  /* 标题文字 */
--gray-900: #111827  /* 主要操作背景 */
```

#### 1.2 语义颜色 (Semantic Colors)
```css
/* 信息传递 */
--white: #ffffff           /* 纯白背景 */
--black-opacity-50: rgba(0, 0, 0, 0.5)  /* 遮罩 */
```

**使用规则**:
- **背景**: `bg-white` (主区域), `bg-gray-50` (次要区域)
- **文字**: `text-gray-900` (标题), `text-gray-600` (正文), `text-gray-500` (辅助)
- **边框**: `border-gray-200` (分隔线), `border-gray-300` (输入框)
- **主要按钮**: `bg-gray-900`
- **次要按钮**: `bg-white` + `border-gray-300`

### 2. 字体系统 (Typography)

#### 2.1 字体族 (Font Family)
```css
/* 默认字体 */
font-family: system-ui, -apple-system, BlinkMacSystemFont,
             'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

/* 代码字体 */
font-family: 'SF Mono', Monaco, 'Cascadia Code',
             'Roboto Mono', Consolas, 'Courier New', monospace;
```

#### 2.2 字体大小 (Font Size)
```css
--text-xs: 0.75rem      /* 12px - 标签、时间戳 */
--text-sm: 0.875rem     /* 14px - 副标题、描述 */
--text-base: 1rem       /* 16px - 正文、输入框 */
--text-md: 1rem         /* 16px - 区块标题 */
--text-lg: 1.125rem     /* 18px - 页面标题 */
--text-xl: 1.25rem      /* 20px - 大标题 */
--text-2xl: 1.5rem      /* 24px - 主标题 */
--text-6xl: 3.75rem     /* 60px - 装饰图标 */
```

#### 2.3 字重 (Font Weight)
```css
font-normal: 400        /* 常规文字 */
font-medium: 500        /* 中等强调 */
font-semibold: 600      /* 标题、重要文字 */
```

#### 2.4 行高 (Line Height)
- **默认**: 1.5 (150%)
- **紧凑**: 1.25 (125%) - 用于大标题
- **文本域**: 自动计算

#### 2.5 字母间距 (Letter Spacing)
- **默认**: normal
- **标签**: `uppercase tracking-wide` (全大写 + 加宽)

### 3. 间距系统 (Spacing)

#### 3.1 内边距 (Padding)
```css
--p-2: 0.5rem    /* 8px - 小按钮 */
--p-3: 0.75rem   /* 12px - 中等按钮 */
--p-4: 1rem      /* 16px - 卡片、容器 */
--p-6: 1.5rem    /* 24px - 页面区域 */
--p-8: 2rem      /* 32px - 大容器 */
```

**特殊组合**:
```css
px-4 py-3   /* 水平 16px, 垂直 12px - 输入框 */
px-6 py-4   /* 水平 24px, 垂直 16px - 主按钮 */
```

#### 3.2 外边距 (Margin)
```css
--space-y-2: 0.5rem    /* 8px */
--space-y-3: 0.75rem   /* 12px */
--space-y-4: 1rem      /* 16px */
--space-y-6: 1.5rem    /* 24px - 表单字段间距 */
```

#### 3.3 间隙 (Gap)
```css
--gap-2: 0.5rem    /* 8px - 标签组 */
--gap-3: 1rem      /* 16px - 按钮组 */
```

### 4. 圆角系统 (Border Radius)

```css
--rounded-sm: 0.125rem  /* 2px - 小圆角 */
--rounded: 0.25rem      /* 4px - 默认圆角 */
--rounded-md: 0.375rem   /* 6px - 中等圆角 - 按钮、输入框 */
--rounded-lg: 0.5rem     /* 8px - 大圆角 - 卡片 */
```

**Notion 风格**: 卡片列表项不使用圆角 (保持矩形)

### 5. 阴影系统 (Box Shadow)

```css
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05)
/* 轻微阴影 - 卡片、按钮 */

--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1)
/* 中等阴影 - 悬停状态 */

--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1)
/* 大阴影 - 模态框、下拉菜单（未使用）*/
```

**使用规则**:
- **卡片**: `shadow-sm`
- **按钮默认**: `shadow-sm`
- **按钮悬停**: `shadow-md`

### 6. 过渡动画 (Transitions)

```css
--transition-colors: color 150ms ease-in-out
--transition-all: all 300ms ease-in-out
```

**应用场景**:
- **颜色过渡**: 按钮背景、文字颜色
- **全部过渡**: 卡片悬停、侧边栏滑入

---

## 设计决策记录

### 1. 为什么选择 Notion 风格？
**决策**: 使用简洁线条和卡片设计，而非阴影卡片

**原因**:
- Notion 是高效工具的代表，符合产品定位
- 线条分隔比阴影更轻量，减少视觉噪音
- 用户熟悉度高，降低学习成本
- 易于实现和维护

**权衡**:
- ✅ 优势: 简洁、快速、易扩展
- ❌ 劣势: 缺少视觉层次，需要通过颜色和字体大小补充

### 2. 为什么使用灰色而非彩色？
**决策**: 主要操作按钮使用深灰色 `bg-gray-900`，而非蓝色或绿色

**原因**:
- 灰色更专业，适合内容创作工具
- 避免颜色分散用户注意力
- 符合小红书的品牌调性（简洁、优雅）
- 易于与用户生成的内容搭配

**权衡**:
- ✅ 优势: 专业、百搭、不抢注意力
- ❌ 劣势: 缺少视觉冲击，需要通过其他方式引导用户

### 3. 为什么侧边栏固定 320px？
**决策**: 侧边栏固定宽度，而非响应式百分比

**原因**:
- 保证列表项的可读性（标题不会被压缩）
- 适配主流桌面分辨率 (1366x768 及以上)
- 移动端使用全屏侧边栏，保证可用性

**权衡**:
- ✅ 优势: 稳定的布局，易于设计
- ❌ 劣势: 小屏幕可能占用过多空间

### 4. 为什么使用原生确认对话框？
**决策**: 使用 `window.confirm()` 而非自定义模态框

**原因**:
- 快速实现，减少代码复杂度
- 原生对话框在不同操作系统上有一致体验
- 不需要额外的模态框组件

**权衡**:
- ✅ 优势: 快速、轻量、一致性好
- ❌ 劣势: 样式无法自定义，体验不够精致

**未来改进**: 如果产品需求提升，可以替换为自定义模态框

### 5. 为什么实时预览而非手动触发？
**决策**: 内容变化时自动更新预览

**原因**:
- 减少操作步骤，提升效率
- 即时反馈，帮助用户快速迭代
- 符合现代 Web 应用的期望

**权衡**:
- ✅ 优势: 用户体验好，效率高
- ❌ 劣势: 可能频繁渲染，性能优化（已使用防抖）

---

## 可访问性 (Accessibility)

### 1. WCAG 合规性

#### 1.1 颜色对比度
- **正文文字** (14px, gray-600 on white): 对比度 8.59:1 ✅ (AAA)
- **小文字** (12px, gray-500 on white): 对比度 5.74:1 ✅ (AA)
- **按钮文字** (18px, white on gray-900): 对比度 15.2:1 ✅ (AAA)

#### 1.2 键盘导航
- **Tab 键**: 所有交互元素可访问
- **焦点可见**: `focus:ring-2 focus:ring-gray-400`
- **焦点顺序**: 符合逻辑（从上到下，从左到右）

#### 1.3 屏幕阅读器
- **ARIA 标签**: 所有按钮有 `aria-label`
- **ARIA 状态**: 菜单按钮有 `aria-expanded`
- **语义化 HTML**: 使用正确的 HTML5 元素

### 2. 触摸目标尺寸
所有可点击元素至少 44x44px:
- ✅ 按钮: `minWidth: '44px', minHeight: '44px'`
- ✅ 输入框: `py-3` (48px 高度)
- ✅ 删除按钮: 明确设置触摸区域

### 3. 焦点管理
- **模态框**: 未使用，无需焦点陷阱
- **侧边栏**: 移动端打开时，背景遮罩阻止焦点穿透
- **表单**: 提交后焦点返回到表单顶部

---

## 性能优化

### 1. 渲染优化

#### 1.1 虚拟化列表
- **未实现**: 当前会话数量少 (<100)，无需虚拟化
- **未来考虑**: 如果会话数 >1000，使用 `react-window` 或 `react-virtualized`

#### 1.2 防抖和节流
- **主题推荐**: 内容变化时实时计算（防抖 300ms）
- **图片捕获**: 无需防抖（用户手动触发）
- **窗口调整**: 无需优化（无布局计算）

#### 1.3 代码分割
- **动态导入**: 未使用（应用体积小）
- **懒加载组件**: 未使用（首屏加载已很快）

### 2. 资源优化

#### 2.1 图片优化
- **格式**: PNG（无损压缩）
- **尺寸**: 根据内容动态计算
- **存储**: Local Storage (限制 5MB)

#### 2.2 字体优化
- **系统字体**: 使用系统默认字体，无需加载
- **代码字体**: 等宽字体优先使用系统内置

#### 2.3 CSS 优化
- **Tailwind CSS**: 使用 PurgeCSS 去除未使用的样式
- **内联样式**: 最小化内联样式，优先使用类名

---

## 未来改进

### 1. 短期改进 (1-2 周)
- [ ] 添加卡片模板库（预设内容）
- [ ] 支持图片上传（从本地上传到卡片）
- [ ] 添加批量导出功能
- [ ] 优化移动端触摸体验

### 2. 中期改进 (1-2 月)
- [ ] 支持拖拽排序卡片
- [ ] 添加协作功能（分享链接）
- [ ] 支持主题自定义（颜色、字体）
- [ ] 添加导出历史记录

### 3. 长期改进 (3-6 月)
- [ ] 支持视频卡片（短视频格式）
- [ ] 添加 AI 内容生成
- [ ] 支持云端存储（跨设备同步）
- [ ] 添加卡片市场（买卖模板）

---

## 附录

### A. Tailwind 类名速查

#### 布局
```css
flex           /* 弹性盒子 */
flex-col       /* 垂直布局 */
flex-1         /* 占据剩余空间 */
h-full         /* 100% 高度 */
w-full         /* 100% 宽度 */
min-h-screen   /* 最小高度 100vh */
```

#### 间距
```css
p-4            /* 内边距 16px */
px-6 py-4      /* 水平 24px, 垂直 16px */
space-y-6      /* 子元素垂直间距 24px */
gap-3           /* 间隙 16px */
```

#### 颜色
```css
bg-white        /* 白色背景 */
bg-gray-50      /* 浅灰背景 */
text-gray-900   /* 深灰文字 */
border-gray-200 /* 浅灰边框 */
```

#### 文字
```css
text-sm         /* 14px */
text-lg         /* 18px */
font-semibold   /* 600 字重 */
text-center     /* 居中对齐 */
truncate        /* 超出省略号 */
```

#### 交互
```css
hover:bg-gray-50     /* 悬停背景 */
focus:ring-1         /* 焦点环 */
transition-colors    /* 颜色过渡 */
cursor-not-allowed   /* 禁止光标 */
```

### B. 常用组件模式

#### 卡片容器
```jsx
<div className="bg-white rounded-lg shadow-sm p-6">
  {/* 内容 */}
</div>
```

#### 按钮
```jsx
<button className="w-full bg-gray-900 text-white font-semibold py-4 px-6 rounded-md hover:bg-gray-800 transition-colors">
  按钮文字
</button>
```

#### 输入框
```jsx
<input
  type="text"
  className="w-full px-4 py-3 border border-gray-300 rounded-md focus:outline-none focus:ring-1 focus:ring-gray-400"
  placeholder="占位符"
/>
```

#### 列表项
```jsx
<div className="p-4 border-b border-gray-100 hover:bg-gray-50 transition-colors cursor-pointer">
  {/* 内容 */}
</div>
```

### C. 设计资源

#### 图标库
- **Heroicons**: https://heroicons.com/ (推荐，与 Tailwind 同一套系)
- **Lucide**: https://lucide.dev/ (轻量级替代)
- **Feather Icons**: https://feathericons.com/ (极简风格)

#### 字体
- **系统字体**: 无需下载
- **代码字体**: SF Mono (macOS), Consolas (Windows)

#### 颜色工具
- **Coolors**: https://coolors.co/ (配色方案生成)
- **Tailwind Shades**: https://www.tints.dev/ (灰度色板生成)

---

**文档维护**: 本文档应随产品迭代同步更新，确保设计实现与文档描述一致。

**最后审核**: 2026-03-18
**下次审核**: 产品重大版本更新时
